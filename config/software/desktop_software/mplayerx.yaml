---
name: MPlayerX
cask: mplayerx
plist:
  domain: org.niltsh.MPlayerX
  mode: '0600'
  values:
    # Check for updates automatically? / Check Automatically
    SUEnableAutomaticChecks: true
    # Preferences / General / Keep window on top / Never
    OnTopMode: 0
